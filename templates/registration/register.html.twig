{% extends 'base.html.twig' %}

{% block title %}SignUp{% endblock %}

{#{% block stylesheets %}#}
{#    <link rel="stylesheet" href="{{ asset('styles/singup.css') }}">#}
{#{% endblock %}#}

{% block body %}
    <body class="body-register">
    <main class="form-signup">
        <h1 class="h3 mb-3 fw-normal">Please sign up</h1>

        {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.email) }}
            {{ form_row(registrationForm.plainPassword) }}
    {#        {{ form_row(registrationForm.agreeTerms) }}#}

            <button class="w-100 btn btn-lg btn-primary mt-2" type="submit">Sign up</button>
        {{ form_end(registrationForm) }}
        <div class="form-floating mt-2">
            <p class="login-wrapper-footer-text">Already have an account? <a href="{{ path('app_login') }}">Signin here</a></p>
        </div>
    </main>
    </body>
{% endblock %}
